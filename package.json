{
  "name": "dental-motion-atelier",
  "version": "1.0.0",
  "private": true,
  "description": "Luxury landing page for dental motion graphic video services.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.spec.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "pg": "^8.20.0"
  }
}
